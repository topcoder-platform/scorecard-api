version: '3'
services:
  dynamodb:
    image: "amazon/dynamodb-local"
    ports:
      - 8000:8000
  elasticsearch:
    image: elasticsearch:6.8.18
    environment:
      - discovery.type=single-node
    ports:
      - 9200:9200